global:
  scrape_interval: 15s

scrape_configs:
  - job_name: 'django_prometheus'
    metrics_path: '/metrics'
    static_configs:
      - targets: ['172.18.0.10:8000']# Using static IP for now due to DNS resolution issue in shared bridge network.
